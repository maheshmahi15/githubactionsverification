name: hello-world
'on': push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "Hello World!"
      - name: HTML Preview
        id: html_preview
        uses: htmlpreview/htmlpreview.github.com@v2
        with:
          html_file: 'index.html'
